<div class="container-fluid">
    <table class="table table-striped table-hover">
        <tbody>
        {{#each productType in controller}}
            <tr>
                <td>
                    {{productType.name}}
                    <i class="fa fa-times-circle pull-right" {{action 'deleteProductType' category}} title="Delete"></i>
                    <i class="fa fa-edit pull-right" {{action 'editProductType' category}} title="Edit"></i>
                </td>
            </tr>
        {{/each}}
        </tbody>
    </table>

    <form class="pull-right">
        <button {{action 'newProductType'}} class="btn btn-default">Add product type</button>
    </form>
</div>

{{outlet}}